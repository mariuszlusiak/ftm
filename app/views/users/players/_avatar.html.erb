<% player = avatar %>
<%= show_avatar(player, :thumb) %>
<br />
<% if player.avatar %>
	<%= 
		link_to_remote 'Usuń',
			:url => remove_avatar_user_player_path(@user, player),
			:method => :delete
	%>
<% else %>
	<%= link_to 'Dodaj', avatar_user_player_path(@user, player) %>
<% end %>
