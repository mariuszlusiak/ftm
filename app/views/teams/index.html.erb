<h1>Lista drużyn</h1>

<% if show_page_info? %>
	<div id="page-info">
		Ta strona przedstawia listę drużyn dodanych przez Ciebie do systemu.
	</div>
<% end %>

<table>
  <tr>
    <th>Nazwa</th>
    <th>Miejscowość</th>
  </tr>

<% @teams.each do |team| %>
  <tr>
    <td><%=h team.name %></td>
    <td><%=h team.city %></td>
    <td><%= link_to 'Szczegóły', team_path(team) %></td>
    <td><%= link_to 'Edytuj', edit_team_path(team) %></td>
    <td>
			<%= link_to 'Usuń', team_path(team),
				:confirm => 'Czy na pewno trwale usunąć drużynę?', :method => :delete %>
		</td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nowa drużyna', new_team_path %>
